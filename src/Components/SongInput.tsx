import { Button } from "@mantine/core";
import { TextInput } from '@mantine/core';

export default function SongInput() {
    return(
        <TextInput
            description="Şarkı ismini girin"
            sx={{backgroundColor:"white"}}
    />
    );
}
